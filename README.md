# like 1000th attempt at this

## windows:

Setup is just running `python ./scripts/dl_cef.py`.

Then to build and use/test just run `cargo run; ./scripts/move.ps1` (move script requires non-ms store install)

## posix:

just gonna use a simple `LD_PRELOAD`/`DYLD_INSERT_LIBRARIES` thing for it lol so simple

coming soon
